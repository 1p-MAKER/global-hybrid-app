import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.globalhybridapp',
  appName: 'GlobalHybridApp',
  webDir: 'out'
};

export default config;
